$kind: http-request
name: Get Patient by ID
method: GET
url: "{{baseUrl}}/patients/{{patient_id}}"
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 2000
description: Retrieve a specific patient's details including their scans. Returns 404 if not found.
